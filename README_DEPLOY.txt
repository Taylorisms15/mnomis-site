v9 GitHub package

Fixes
- Logo is larger (40px) and uses one stable path: assets/mnomis-logo.png
- All pages are real pages (no placeholder copy)

Required action
- Put your real logo file at assets/mnomis-logo.png (lowercase, no spaces)
- Delete root logo duplicates like "Mnomis Logo.png" and "mnomis-logo.png" to avoid confusion

Deploy
- Unzip into repo root so index.html is at the root
- Commit and push
